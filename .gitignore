# Node modules
node_modules/
# Next.js build output
.next/
out/
# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Firebase service secrets
firebase-service-account.json
firebase-key.json
# Log files
*.log
npm-debug.log*
yarn-debug.log*
# System files
.DS_Store
Thumbs.db
# IDE settings
.vscode/
.idea/
# Test databases and caches
*.sqlite
*.db
.cache/
# Uploads or user files (if any)
uploads/
# Data folders (if you don't want src/data/ público)
src/data/
# Context (if você não quer contexto sensível público)
src/context/
# Firestore local config (se existir)
firestore.indexes.json
firestore.rules
# Outros arquivos sensíveis ou que não devem ir pro repo
secret.json
*.env
*.local
*.json
firebase-*.json
